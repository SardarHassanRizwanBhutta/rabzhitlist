import { Candidate } from "@/lib/types/candidate"

/**
 * Placeholder for candidate list data. Use backend APIs (e.g. candidates-api) for CRUD;
 * pass fetched candidates into page/client components instead of this.
 */
export const sampleCandidates: Candidate[] = []
