/** Visible name cap in the candidates list (table and cards). */
export const CANDIDATE_LIST_NAME_MAX_SAMPLE = "Muhammad Abdullah Ahmad"

/** Visible job title cap in the candidates list cards header row. */
export const CANDIDATE_LIST_JOB_TITLE_MAX_SAMPLE = "Senior Software Engineer"

/** Visible city cap in the candidates list (table and cards). */
export const CANDIDATE_LIST_CITY_MAX_SAMPLE = "Rawalpindi"

/** Visible office/campus location match badge cap in candidates cards. */
export const CANDIDATE_MATCH_OFFICE_LOCATION_BADGE_MAX_SAMPLE =
  "Karachi — Main Street, Area"

/** Alias for campus location badges (same City — Address format). */
export const CANDIDATE_MATCH_CAMPUS_LOCATION_BADGE_MAX_SAMPLE =
  CANDIDATE_MATCH_OFFICE_LOCATION_BADGE_MAX_SAMPLE

/** Exported for candidates list views; width comes from `CANDIDATE_LIST_NAME_MAX_SAMPLE`. */
export const CANDIDATE_LIST_NAME_TRUNCATE_CLASS = ""

/** Exported for candidates list views; width comes from `CANDIDATE_LIST_JOB_TITLE_MAX_SAMPLE`. */
export const CANDIDATE_LIST_JOB_TITLE_TRUNCATE_CLASS = "block w-full min-w-0 max-w-full"

/** Exported for candidates list views; width comes from `CANDIDATE_LIST_CITY_MAX_SAMPLE`. */
export const CANDIDATE_LIST_CITY_TRUNCATE_CLASS = "block w-full min-w-0 max-w-full"
